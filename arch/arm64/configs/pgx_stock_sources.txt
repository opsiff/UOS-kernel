pgx_stock 构建用源码来源审计（对照 android_kernel_huawei_kirin9000 = 5.10.43 / baltimore）

一、与 kirin9000 逐字节一致，无需回修
  platform_source/basicplatform/drivers/scsi/ufs/hufs-lib.c
  platform_source/basicplatform/drivers/bootctrl/bootctrl.c / bootctrl.h / bootctrl_make.h / bootctrl_utility.c
  platform_source/basicplatform/block/blk_scsi_kern.c / mas_blk_inline_crypt.c / mas_blk_tz.c / mas_blk_iosched_interface.c
  kernel/sched/core_ctl.c：本树补的 hisi_rtg.h 包含与 kirin9000 第 30 行一致

二、已改为 kirin9000 来源
  platform_source/basicplatform/block/mas_blk_mq_tag.c      替换原 clang 汇编 blob
  platform_source/see/drivers/bl31_hibernate/*              原先取自 kernel-pgx 并已撤销，改自 kirin9000

三、kirin9000 没有，保留现有来源（待找齐厂商源码）
  platform_source/basicplatform/block/mas_blk_busy_idle_notify.c / mas_blk_core.c / mas_blk_dump.c /
      mas_blk_flush.c / mas_blk_iosched_ufs_mq.c / mas_blk_latency.c      来源：android_kernel_LIO_kirin990_103
  include/platform_include/basicplatform/linux/mas/mas_blk.h 等头文件     来源：LIO 对齐 + 本树补 mas_blk_register_queue 声明
  net/bluetooth/hci_core.c 的 hisi_hci_dev_do_open（k9000 同样没有定义，仅 pgx 有）
  drivers/hisi/hi1103_eeprom（drv_e2prom_read，仅 pgx 有实现）
  kernel/time/alarmtimer.c 的 poweroff alarm 守卫（k9000 无守卫，本树按 extern 声明 + 分配置定义实现）

多开项对照（相对机器全量配置 config-5.10.97，共 17 项）

A. 主动打开（6 项；本树源码前提，机器内核用更新版源码达成同一效果）
  LOG_EXCEPTION / LOG_USERTYPE     提供者侧：本树 zrhung_event.c 调用 hiview_send_hisysevent 无配置守卫
  CHR_NETLINK_MODULE               提供者侧：net/wireless/sme.c 调用 wifi_disconnect_report 无配置守卫
  HMDFS_ANDROID / HMDFS_CRYPTO / HMDFS_XATTR_NOSECURITY_SUPPORT   hmdfs 源码前提（机器内核的 hmdfs 更新版不需要）
  等价性：运行时默认行为一致，只是把机器侧由新版源码隐式编入的代码显式打开。

B. 能力位（5 项；架构/工具链探测，无 prompt，配置层不可关）
  ARCH_WANTS_IRQ_RAW                   arch/arm64/Kconfig:115  select
  HAVE_MOVE_PUD                        arch/arm64/Kconfig:132  select
  HAVE_ARCH_KFENCE                     arch/arm64/Kconfig:145  select
  ARCH_SUPPORTS_SPECULATIVE_PAGE_FAULT arch/arm64/Kconfig:213  select
  AS_HAS_LDAPR                         arch/arm64/Kconfig:1429 def_bool $(as-instr,.arch_extension rcpc)
  等价性：纯能力位；其对应功能符号 SPECULATIVE_PAGE_FAULT 在本树为 n，与机器一致；零行为差。

C. 不可见 / choice 默认成员（3 项；Kconfig 层不可关闭，scripts/config 对不可见符号无效）
  CRYPTO_FIPS140               crypto/Kconfig:38  def_bool y（无 prompt，依赖 MODULES && ARM64 && ARM64_MODULE_PLTS 满足即 y）
  EROFS_FS_ZIP_CACHE_BIPOLAR   fs/erofs_4_14/Kconfig:141  choice 的默认成员
  MQ_IOSCHED_DEADLINE_CGROUP   block/Kconfig.iosched:12  tristate 无 prompt + default y（依赖 MQ_IOSCHED_DEADLINE、BLK_CGROUP 满足即 y）
  等价性：名义多开；对应功能仅在显式选择（挂载选项、调度器切换）时生效，默认启动路径无差异。

D. 取值不同（1 项）
  FONT_8x16=y（机器 FONT_CJK_16x16=y，而本树没有该字体驱动）→ 仅影响文本控制台字形，桌面阶段字体由用户态接管。

E. 代码要求（1 项）
  OEM_DEFINE_VERITY_FEC   drivers/md/dm-verity-target.c 无条件使用 FIRST_TIME_HASH_VERIFY（详见构建例外清单）

F. 启动相关（1 项）
  CMDLINE_FROM_BOOTLOADER 与 pgx 树语义一致（详见构建例外清单）

合计 6 + 5 + 3 + 1 + 1 + 1 = 17 项，与当前多开数量一致。

少开项分类（相对机器全量配置 config-5.10.97，共 139 项 = 例外主动关闭 26 + 本树未打开 113）

修正记录（2026-09-14）：早期把这 113 项统称“本树 Kconfig 无此符号”不准确，原因有二：
  1) 只搜索了名为 Kconfig 的文件，漏掉 Kconfig.* 与子目录 Kconfig（同类错误此前也发生在 MQ_IOSCHED_DEADLINE_CGROUP）；
  2) 用“符号未出现在 .config”替代了“符号在 Kconfig 中无定义”，而前者也可能是依赖不满足。
正确口径复核结果：39 项本树有定义、74 项确实没有定义。

一、例外主动关闭 26 项：见构建例外清单（缺源码或缺依赖）；HMDFS_FS 已补齐并打开。

二、本树未打开 113 项的细分

A. 本树有 Kconfig 定义、只是未打开：39 项
   A1 被例外清单的父项间接阻塞（11 项）
      DPU_DRM_GEM(父 DPU_DRM)、ARCH_HIBERNATION_HEADER / HIBERNATION_SNAPSHOT_DEV /
      HISI_BL31_HIBERNATE / HIBERNATE_CALLBACKS(父 HIBERNATION)、ECRYPT_FS_MESSAGING(父 ECRYPT_FS)、
      HUGETLB_PAGE(父 HUGETLBFS)、ARCH_ENABLE_HUGEPAGE_MIGRATION(依赖 HUGETLB_PAGE)、
      SCSI_UFS_CUST_MAX_SECTORS(依赖 MAS_BLK)、HW_VOTE_L3C_FREQ(依赖 L3C_DEVFREQ)、
      L3C_DEVFREQ(依赖 ARM_DSU_PMU 与 NAMTSO_PMU，二者未开)
   A2 依赖已满足、父菜单未开（22 项）
      HI1102_PLATFORM 与 HI1102_PLAT_FRW/HW_CHR/MAIN/OAL/OAM/PM/PS/SDT（9）、
      HI1102_WIFI 与 _HMAC/_MAIN/_WAL（4）、HI110X_SDIO_DETECT_FUNCTION/FPGA/STD_CARD_SUPPORT（3）、
      DRM_GEM_CMA_HELPER / DRM_KMS_CMA_HELPER / DRM_MIPI_DSI、PERFHUB / PERF_CTRL / PERF_MODE、
      CRYPTO_SIMD、ARCH_MIGHT_HAVE_PC_PARPORT
   A3 平台条件不满足（4 项）
      PARPORT_PC（依赖 ARCH_MIGHT_HAVE_PC_PARPORT；arm64 不 select，而机器侧的树有该 select）、
      PARPORT_PC_FIFO、PARPORT_PC_PCMCIA、PARPORT_SERIAL
   源码实况抽查：
      - platform_source/display/drivers/dpu_drm：Kconfig 与 Makefile 在，但 .c 文件为 0（源码缺）→
        即使打开父项 DPU_DRM 也编不了，显示整块仍需从 kernel-pgx 取代码
      - drivers/connectivity/hi11xx/hi1105/{platform,wifi}：.c 分别为 159 与 264 个（代码齐）→
        打开父项 CONNECTIVITY_HI110X 即可参与构建
      - drivers/parport：16 个 .c（代码齐，仍需 arm64 select 该能力位）
      - platform_source/cee/drivers/{perfhub,l3c_devfreq}：.c 为 0（缺源码）

B. 本树没有 Kconfig 定义：74 项
   B1 Makefile 已引用该符号（代码或构建规则在树里，只缺 Kconfig 定义）：4 项
      DFX_OHOS(drivers/hwmemcheck)、HISI_MODEM_OHOS(drivers/platform_drivers/modem)、
      MM_LTS_KERNEL(platform_source/basicplatform/mm)、USB_HW_F_ACM_MUSL(drivers/huawei_platform/usb)
   B2 无任何 Makefile 引用（基本可判定为缺代码；个别如 KVM_ARM_PMU 是 arch/arm64/kvm 代码在但构建项与 Kconfig 被裁）：70 项
      64BIT_CCSIDR, ANDROID_STRUCT_PADDING, ARCH_SUPPORTS_CFI_CLANG, ARMPC_ECHUB_FAN,
      ARMPC_ECHUB_MCU_STATE_SYNC, ARMPC_ECHUB_OVERHEAT, BINDER_ONEWAY_TF_SENDER_INFO,
      BINDER_SENDER_INFO, BLACKBOX, CPU_FREQ_DEFAULT_GOV_DDE_SCHEDUTIL, CRYPTO_CORE_DISABLE,
      DEVICE_THREAT_DETECTION, DPU_CONN_MANAGER, DPU_DKSM, DPU_DP_CTRL, DPU_DRM_BRIDGE, DPU_DRM_DP,
      DPU_HDCPTX, DP_DSC, EDP_DRIVER, FBE_ARMPC, FONT_CJK_16x16, GD25L_DRIVER, HIEVENT,
      HISI1103_EEPROM, HMDFS_FS_ENCRYPTION, HMDFS_FS_PERMISSION, HUAWEI_ARMPC_SMART_USB,
      HUAWEI_HIBERNATE_SWAP_PROTECT, IP_RSVD_PORT_BIND_CTRL, KVM_ARM_MPAM_BUGFIX, KVM_ARM_PMU,
      L3CACHE_TZSP, LEDS_ARMPC, NAMTSO, NAMTSO_PMU, NET_VENDOR_MOTORCOMM, NET_VENDOR_WANGXUN,
      NGBE, NGBEVF, QOS_SCHED_DYNAMIC_AFFINITY, R8168, R8168_NAPI, RTL8821CU, RTL8822BU,
      RTW88_8723DU, RTW88_8723X, RTW88_8821CU, RTW88_8822CU, RTW88_USB, RT_SOFT_BANDWIDTH,
      SCHED_EAS, SCSI_UFS_LUN_PROTECT, SECURITY_ELFVERIFY, SECURITY_HOOKMANAGER,
      SECURITY_UOS_MODULE_PROTECT, SECURITY_UOS_SECURITY_MODULES, SELINUX_SID2,
      SR_HIBERNATION_SUSPEND_TIME_OPP, TCP_NB_URC, THERMAL_THRESHOLD, TXGBE, TXGBEVF,
      UNIFIED_COLLECTION, UOS_INVENTION, UOS_LIGHTCHERRY, UOS_PROC, UOS_TOUCHPAD_SWITCH,
      USB_REMOTE_WAKEUP, YT6801
